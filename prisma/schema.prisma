generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Ticket {
  id       Int    @id @default(autoincrement())
  title    String
  status   String
  priority String
  owner    String
}
